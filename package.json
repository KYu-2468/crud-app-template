{
  "name": "crud-app-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"nodemon server\" \"npm start --prefix ./client\"",
    "build": "npm run build --prefix ./client",
    "client": "npm start --prefix ./client",
    "install:server": "npm i",
    "install:client": "npm install --prefix ./client",
    "install:all": "npm run install:server && npm run install:client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.1",
    "sequelize": "^6.32.1"
  }
}
